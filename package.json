{
  "scripts": {
    "start": "cd api-server/ && npm install && node --experimental-specifier-resolution=node src/index.js",
    "build": "cd client/ && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^5.2.0"
  }
}
